//array numbers comparision; 
#include <stdio.h>
int main(){
    int n; 
    scanf("Enter the no  elements in the array %d", &n);
    int arr[n];
    int i;
    
    for(i=0; i<n;i++);{
        printf("Element array[%d]:" , i++);
        scanf("%d", &arr[i++]);
}
return 0; 
}